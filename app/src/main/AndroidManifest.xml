<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.spendingmanagement" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon_change"
        android:label="Quản Lý Tài Chính Cá Nhân"
    android:supportsRtl="true"
        android:theme="@style/Theme.SpendingManagement"
        tools:targetApi="31" >
        <activity
            android:name=".MainActivity2"
            android:exported="true"
            android:label="Quản lý chi tiêu cá nhân"
            android:theme="@style/Theme.SpendingManagement.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".model.PhanLoaiActivity" >
        </activity>

    </application>

</manifest>